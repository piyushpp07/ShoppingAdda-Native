import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const Cart = () => {
    return (
        <View style={{ flex: 1 }}>
            <Text>Cart Page</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default Cart;
