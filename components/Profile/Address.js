import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const Address = () => {

    return (
        <View>
            <Text>this is address section</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default Address;
